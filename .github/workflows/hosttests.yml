name: Host tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  hosttests:
    name: Build and run host tests
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run host tests
        run: make hosttests
